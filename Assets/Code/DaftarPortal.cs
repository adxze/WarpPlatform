using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DaftarPortal : MonoBehaviour
{
    // Start is called before the first frame update
    public GameObject[] Portals;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
